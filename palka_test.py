# -*- coding: utf-8 -*-
"""ТЕСТ-НАБОР механизма «палка» v2 — БЕЗ ШТИФТОВ (идея юзера):
во всех шарнирах печатный ШТЫРЁК Ø5 в одной детали и ЯМКА Ø6 в другой.
Запуск: .venv-b123d\\Scripts\\python palka_test.py
Сборка: всё просто надевается сверху. Кривошипов три (r6 / 6.5 / 7) —
регулировка хода выбором детали.
"""
from build123d import *
import math as _m

OUT = r"C:\App\gitar\2-0\Print\Print"
STUD = 2.5                                     # штырёк Ø5 (не мельчим!)
HOLE = 3.0                                     # ямка Ø6 — зазор 1 мм, крутится свободно


def BB(x0, x1, y0, y1, z0, z1):
    return Pos((x0 + x1) / 2, (y0 + y1) / 2, (z0 + z1) / 2) * Box(
        abs(x1 - x0), abs(y1 - y0), abs(z1 - z0))


def spline_cut(h, t=0.0):
    cut = Pos(0, 0, h / 2) * Cylinder(2.45 + t, h + 0.2)
    for k in range(10):
        a = k * 36
        r = _m.radians(a)
        cut += Pos((2.4 + t) * _m.cos(r), (2.4 + t) * _m.sin(r), h / 2) * \
               Rot(0, 0, a) * Box(1.3, 0.95 + 2 * t, h + 0.2)
    return cut


# ---------------- плита-стенд ----------------
plate = BB(0, 255, -35, 35, 0, 3)
plate += BB(16, 20, -35, 35, 3, 9)             # рельса канала (цельная, запад)
plate += BB(40, 44, 18, 35, 3, 9)              # восточная рельса: два огрызка,
plate += BB(40, 44, -35, -26, 3, 9)            # между ними ПРОЁМ для плеча
plate += BB(20, 44, -35, 35, 3, 5.0)           # НАСТИЛ канала (подложка юзера):
plate += BB(44, 90, -8.5, 4.5, 3, 5.0)         # плечо и тележка ЛЕЖАТ, не висят
plate += BB(193, 207, -30, -14, 3, 8.2)        # столик под шатун (до заглушки)
plate += Pos(95, -2, 4) * Cylinder(5.5, 2)     # стойка оси треугольника
plate += Pos(95, -2, 6.2) * Cylinder(STUD, 2.4)  # БУГОРОК оси (Ø4)
for gx in (130, 172):                          # губки: паз 6.4 (спица 5 + зазор)
    plate += BB(gx - 4, gx + 4, -27.6, -26.4, 3, 12)
    plate += BB(gx - 4, gx + 4, -20.0, -18.8, 3, 12)
    plate += BB(gx - 4, gx + 4, -26.4, -20.0, 3, 6.4)   # подоконник под спицей
# КОЛОДЕЦ МОТОРА NEMA14 (мотор снизу, вал вверх): Ø37.4 + уши M3 вдоль X
plate -= Pos(229, -15, 1.5) * Cylinder(18.7, 3.2)
for sgx in (-1, 1):
    plate -= Pos(229 + sgx * 21.925, -15, 1.5) * Cylinder(1.7, 3.2)
# отверстия ножек по углам (шип Ø4.75)
for lx, ly in ((10, -30), (10, 30), (245, -30), (245, 30)):
    plate -= Pos(lx, ly, 1.5) * Cylinder(2.45, 3.2)

# ---------------- треугольник: дырка оси + два бугорка вверх ----------------
tri = Pos(0, 0, 0.8) * Cylinder(11, 1.6)
tri += BB(-66.6, 0, -2.5, 2.5, 0, 1.6)         # плечо 65 к серьге
tri += BB(-2.5, 2.5, -22.8, 0, 0, 1.6)         # плечо 21.2 к спице
tri += Pos(-65, 0, 0.8) * Cylinder(5.5, 1.6)   # площадки под штырьки
tri += Pos(0, -21.2, 0.8) * Cylinder(5.5, 1.6)
tri -= Pos(0, 0, 0.8) * Cylinder(HOLE, 1.8)    # дырка оси (на бугорок стойки)
tri += Pos(0, -21.2, 2.8) * Cylinder(STUD, 2.4)  # бугорок угла-1 (спица)
tri += Pos(-65, 0, 2.35) * Cylinder(STUD, 1.5)   # штырёк tip: заподлицо с серьгой

# ---------------- спица: дырка у головы, бугорок на хвосте ----------------
spica = BB(0, 97, -2.5, 2.5, 0, 1.6)
spica += Pos(4, 0, 0.8) * Cylinder(5.5, 1.6)
spica += Pos(93.5, 0, 0.8) * Cylinder(5.5, 1.6)
spica -= Pos(4, 0, 0.8) * Cylinder(HOLE, 1.8)   # на бугорок угла-1
spica += Pos(93.5, 0, 2.8) * Cylinder(STUD, 2.4)  # бугорок под шатун

# ---------------- шатун: две дырки ----------------
shatun = BB(0, 45, -2.5, 2.5, 0, 1.6)
shatun += Pos(3, 0, 0.8) * Cylinder(5.5, 1.6)
shatun += Pos(42, 0, 0.8) * Cylinder(5.5, 1.6)
shatun -= Pos(3, 0, 0.8) * Cylinder(HOLE, 1.8)
shatun -= Pos(42, 0, 0.8) * Cylinder(HOLE, 1.8)

# ---------------- серьга: ДВЕ дырки (идея юзера) ----------------
serga = BB(0, 16, -4, 4, 0, 1.6)
serga -= Pos(4, 0, 0.8) * Cylinder(HOLE, 1.8)     # на штырёк tip плеча
serga -= Pos(12, 0, 0.8) * Cylinder(HOLE, 1.8)    # на выпуклость тележки

# ---------------- кривошипы (3 шт: r6 / 6.5 / 7) со своим бугорком --------
cranks = []
for rr in (6.0, 6.5, 7.0):
    c = Pos(0, 0, 1.25) * Cylinder(11, 2.5)
    c -= spline_cut(2.5, 0)                    # звёздочка на шестерню мотора
    c += Pos(rr, 0, 5.0) * Cylinder(STUD, 5.0)  # палец-бугорок (высокий)
    cranks.append((rr, c))

# ---------------- тележка: паз снизу для серьги + выпуклость вниз ---------
cart = BB(-9.5, 9.5, -12, 12, 0, 8)
cart -= BB(-9.6, 9.6, -9.5, 9.5, -0.1, 3.5)    # паз ШИРОКИЙ: серьга машет до ±45°
cart += Pos(6, 0, 2.6) * Cylinder(STUD, 1.8)   # ВЫПУКЛОСТЬ вниз — в серьгу

# ---------------- ножки ----------------
leg = Pos(0, 0, 13) * Cylinder(5, 26)
leg += Pos(0, 0, 27.5) * Cylinder(2.35, 3.0)
# ---------------- фальш-мотор: заглушка с пеньком под звёздочку -----------
plug = Pos(0, 0, 1) * Cylinder(20.5, 2)
plug += Pos(0, 0, -1.5) * Cylinder(18.5, 3.1)
plug += Pos(0, 0, 3.4) * Cylinder(2.45, 2.8)   # пенёк Ø4.9 короткий: держит кривошип, под шатуном не торчит

parts = [("ptest_plate_v2", plate), ("ptest_triangle_v2", tri),
         ("ptest_leg_v1", leg), ("ptest_plug_v2", plug),
         ("ptest_spica_v2", spica), ("ptest_shatun_v2", shatun),
         ("ptest_serga_v2", serga), ("ptest_cart_v2", cart)]
parts += [(f"ptest_crank_r{str(rr).replace('.', '')}_v2", c) for rr, c in cranks]
for name, part in parts:
    p = part if isinstance(part, Part) else Part() + part
    export_stl(p, rf"{OUT}\{name}.stl")
    print(f"{name}: volume={p.volume:.0f} mm3, solids={len(p.solids())}")
print("export done")
